/* eslint-disable prettier/prettier */
//dto :-> data transfer object
// in which we are defiining a structure
export interface Book {
  id: string;
  title: string;
  author: string;
  published: string;
}
